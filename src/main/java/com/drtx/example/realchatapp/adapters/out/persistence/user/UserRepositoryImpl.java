package com.drtx.example.realchatapp.adapters.out.persistence.user;

// This class was previously a Spring component and caused a circular dependency
// because it lived in the same package as the Spring Data repository and
// was treated as a custom implementation. It's now left as a stub and the
// real adapter is in `UserPersistenceAdapter`.
class UserRepositoryImpl {
    // placeholder
}
